export default class User {
  constructor(username, email, password, picture, bio) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.picture = picture;
    this.bio = bio;
  }
}
